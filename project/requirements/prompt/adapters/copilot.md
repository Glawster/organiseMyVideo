# GitHub Copilot adapter

Follow `.github/copilot-instructions.md` and
`.github/additional-instructions.md`. Treat the linked requirement as the
authoritative outcome and preserve its exclusions. Propose only changes within
the prompt's named files or components.

Report changed files, criterion-to-evidence mapping, verification results,
assumptions, and unresolved decisions. Do not claim completion without evidence
for every assigned criterion.
